[package]
name = "protocol"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Shared parameter definitions and contracts for VstKit"

# No external dependencies - pure Rust crate
[dependencies]
